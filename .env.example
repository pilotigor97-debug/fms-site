# --------- Multi-tenant routing ---------
# Domínio raiz onde o site institucional está hospedado.
# Em dev: localhost / em prod: opspilot.com.br ou fms-saas.vercel.app
NEXT_PUBLIC_ROOT_DOMAIN=localhost

# URL do app Flutter pra redirect pós-login (handoff).
# Hoje aponta para o mesmo projeto Firebase do site (auth unificada).
NEXT_PUBLIC_APP_URL=https://relatorio-tecnico-dev.web.app

# --------- Firebase ---------
# IMPORTANTE: o site compartilha o mesmo projeto Firebase do app Flutter
# (relatorio-tecnico-dev em dev, relatorio-tecnico-4ae8c em prod). Antes
# era um projeto separado (opspilot-dev) — migrado em 2026-04-28 para que
# users/companies vivam num lugar só.
FIREBASE_PROJECT_ID=relatorio-tecnico-dev

# Web API Key — usada pelo route /api/auth/login pra autenticar
# via REST do Identity Toolkit. Pega em Console → Project Settings
# → General → Web API Key. Está em lib/firebase_options_dev.dart no
# repo do app Flutter. NÃO é segredo absoluto, server-side por organização.
FIREBASE_WEB_API_KEY=your-web-api-key-here

# Service Account do Firebase Admin SDK — gera no Console:
# Project Settings → Service Accounts → Generate New Private Key
# Essas credenciais SÃO segredas — server-side only.
# Em dev local pega de: relatorio_tecnico/scripts/serviceAccountKeyDev.json
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxx@relatorio-tecnico-dev.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...your key here...\n-----END PRIVATE KEY-----\n"

# --------- Sales lead form ---------
# Email pra onde os leads do botão "Falar com vendas" são enviados.
# Reutiliza a extensão firestore-send-email do projeto: enfileira
# em `mail/` collection com kind='salesLead'.
SALES_LEAD_EMAIL=vendas@opspilot.com.br
